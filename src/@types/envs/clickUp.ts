/**
 * ! ClickUp Envs
 *
 * Add here the Environment Variables from the [.env] file that are related to [ ClickUp ]
 */
export interface IClickUpEnvs {
	CLICKUP_TOKEN: string;

	CLICKUP_TEAM_ID: string;
}